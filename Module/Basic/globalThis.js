// Like react everythings knows as a Component as in node everyhtings know as "Module"

// We can use this globaThis obejct in every javascript evnironment like node, browser, etc. It is a global object that provides access to the global scope.

globalThis.console.log("Hello Friends");

// like browser we can't use the window object in node because node run on server side. we will get error if we use window object in node. but we can use window object in browser because browser run on client side.
// window.console.log("Hello Friends");
